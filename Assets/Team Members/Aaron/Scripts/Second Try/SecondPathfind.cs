using UnityEngine;

namespace Team_Members.Aaron.Scripts.Second_Try
{
    public class SecondPathfind : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
